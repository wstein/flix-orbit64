[package]
name        = "orbit64-cli"
description = "Demo command line for orbit64: a size table, a round-trip demo, and a token decoder"
version     = "0.1.0"
repository  = "github:wstein/flix-orbit64"
flix        = "0.75.2"
license     = "Apache-2.0"
authors     = ["Werner Stein"]

[dependencies]
"github:wstein/flix-orbit64" = "0.3.0"
