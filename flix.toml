[package]
name        = "orbit64"
description = "Canonical URL-safe encoding for NxNxN cube states and move sequences"
version     = "0.5.0"
repository  = "github:wstein/flix-orbit64"
modules     = ["Orbit64"]
flix        = "0.75.2"
license     = "Apache-2.0"
authors     = ["Werner Stein"]
